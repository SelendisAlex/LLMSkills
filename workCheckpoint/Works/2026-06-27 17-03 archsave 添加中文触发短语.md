# archsave 添加中文触发短语

- 按用户明确要求修改 `archsave/SKILL.md`，在 `## 2` 标题中为 `"Organize the Work Architecture"` 增加中文触发短语 `"整理工作架构"`。
- 同步更新 frontmatter `description`，让中文短语也参与 Skill 的触发语义，符合“调整触发条件时同步检查 description”的长期规则。
- 本次在 `SKILL.md` 中加入中文短语是用户明确指定的触发词例外；其余新增说明仍保持英文。
- 使用 `PYTHONUTF8=1` 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
- 运行 `git diff --check -- .\archsave\SKILL.md`，仅出现既有 LF/CRLF 换行提示，无 whitespace 错误。
