# archsave 添加今日工作简述触发

- 按用户要求修改 `archsave/SKILL.md`，新增第 3 条，用于处理英文或中文的“简述今天工作”类请求。
- 新规则要求根据当日工作记录，将今日工作结果整理为一段话；若当天工作是大型或重要改动，可以适当加长描述，但保持聚焦结果。
- 同步更新 frontmatter `description`，加入 brief summarize today's work 与中文短语 `"简述今天工作"` 的触发语义。
- 本次在 `SKILL.md` 中加入中文短语是用户明确指定的触发词例外；其余新增说明保持英文。
- 使用 `PYTHONUTF8=1` 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
- 运行 `git diff --check -- .\archsave\SKILL.md`，仅出现既有 LF/CRLF 换行提示，无 whitespace 错误。
