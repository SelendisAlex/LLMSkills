# archsave 启动检查置顶

- 修改 `archsave/SKILL.md`，将 `Mandatory Startup Check` 移动到 `# ArchSave` 后的正文最顶端。
- 保持启动检查内容不变，保留 `Architecture Documentation Workflow` 为第 1 条、显式 `Organize the Work Architecture` 触发说明为第 2 条。
- 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
- 运行 `git diff --check`，仅出现既有 Git 换行提示，无 whitespace 错误。
