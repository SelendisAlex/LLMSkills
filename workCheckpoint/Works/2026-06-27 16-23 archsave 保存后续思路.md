# archsave 保存后续思路

- 按用户要求修改 `archsave/SKILL.md`，在架构文档保存逻辑中加入自动包含后续工作思路的规则。
- 遵守 Skill 文件始终英文维护的长期约束，新增英文描述：保存架构文档时，如有助于后续继续或交接，应包含相关 follow-up work ideas。
- 同步更新 frontmatter `description`，确保新增行为可被 Skill 触发语义覆盖。
- 使用 `PYTHONUTF8=1` 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
- 运行 `rg -n "\p{Han}" .\archsave\SKILL.md`，未发现中文字符。
- 运行 `git diff --check -- .\archsave\SKILL.md`，仅出现既有 LF/CRLF 换行提示，无 whitespace 错误。
