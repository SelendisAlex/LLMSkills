# 保存构建思路和目标状态

- 按用户要求修改 `archsave/SKILL.md` 和 `Skills/checkpointsave/SKILL.md`。
- `archsave/SKILL.md` 新增规则：架构文档应保存项目总体构建思路，按大目标步骤组织，并细分为小目标工作流程，便于随时中断和继续。
- `archsave/SKILL.md` 同步更新 frontmatter `description`，覆盖总体构建思路、大目标、小目标流程、后续思路和交接续做语义。
- `Skills/checkpointsave/SKILL.md` 新增规则：任务结束 checkpoint 应保存当前目标的构建思路、已完成目标和后续应做目标，便于随时中断、继续或交接。
- `Skills/checkpointsave/SKILL.md` 同步更新 frontmatter `description`，覆盖当前目标构建思路、完成目标、后续目标和相关结束提示词语义。
- 遵守 Skill 文件始终英文维护规则，目标 `SKILL.md` 文件未加入中文。
- 使用 `PYTHONUTF8=1` 分别运行 `quick_validate.py .\archsave` 和 `quick_validate.py .\Skills\checkpointsave`，结果均为 `Skill is valid!`。
- 运行 `rg -n "\p{Han}" .\archsave\SKILL.md .\Skills\checkpointsave\SKILL.md`，未发现中文字符。
- 运行 `git diff --check -- .\archsave\SKILL.md .\Skills\checkpointsave\SKILL.md`，仅出现既有 LF/CRLF 换行提示，无 whitespace 错误。
