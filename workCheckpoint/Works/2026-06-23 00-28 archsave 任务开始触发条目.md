# archsave 任务开始触发条目

- 修改 `archsave/SKILL.md` 的 description，加入任务开始读取 `workArchitecture/` 的触发条件。
- 在 `archsave/SKILL.md` 中新增英文条目 `## 2. Automatic Trigger at Task Start`。
- 条目内容要求：当没有本工作区工作记忆，或本轮对话首次执行代码任务、review 或其他重要任务且未读取 `workArchitecture/` 时，读取并记住它。
- 将原“Organize the Work Architecture”条目顺延为第 3 条。
- 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
- 运行 `git diff --check`，仅出现既有 Git 换行提示，无 whitespace 错误。
