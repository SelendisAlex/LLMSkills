# 修正 archsave 英文语句

- 按用户要求将刚刚修改的 Skill 文件语句改为英文。
- 修改 `archsave/SKILL.md`：将 `- 使用中文记录。` 替换为 `- Record architecture documentation in Chinese.`。
- 保持该条语义不变：`archsave` 产出的架构文档仍应使用中文记录，但 Skill 文件自身使用英文维护。
- 使用 `PYTHONUTF8=1` 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
- 运行 `rg -n "\p{Han}" .\archsave\SKILL.md`，未发现中文字符。
