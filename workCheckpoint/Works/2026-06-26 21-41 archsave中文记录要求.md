# archsave 中文记录要求

- 修改 `archsave/SKILL.md` 的 `Architecture Documentation Workflow`。
- 在架构文档工作流说明中加入“使用中文记录。”，明确 archsave 产出的架构记录应使用中文。
- 保持原有 frontmatter、章节结构、触发条件和其它工作流说明不变。
- 使用 `PYTHONUTF8=1` 运行 `quick_validate.py .\archsave`，结果为 `Skill is valid!`。
